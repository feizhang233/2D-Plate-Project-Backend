# 博士面试复习入口

这个项目研究的是：**均匀、各向同性、等厚板在小变形和线弹性条件下，受到横向荷载后的弯曲响应**。

你首先只需要记住下面这一条链：

> 横向荷载 `q` → 等效节点力 `f` → 组装刚度矩阵 `K` → 施加边界条件 → 解 `Ku=f` → 得到挠度和转角 `u` → 恢复曲率、弯矩、剪力和表面应力。

## 一分钟版本

1. 用板的中面代替三维实体，每个节点保留三个自由度：`[w, θx, θy]`。
2. 薄板横向剪切可以忽略，使用 Kirchhoff/DKQ；中厚板需要考虑横向剪切，使用 Reissner–Mindlin/MITC4。
3. Q4 单元把连续的板划分成许多四边形，并用形函数近似单元内部位移。
4. 每个单元通过能量关系得到刚度 `Ke` 和等效荷载 `fe`，再组装成整体 `K` 和 `f`。
5. 约束支座自由度后解线性方程，最后从位移导数恢复内力和应力。
6. 必须检查平衡、刚体模态、patch test、网格收敛、薄板极限和畸变敏感性，不能只看一张云图。

## 项目里最重要的选择

项目使用板厚与板短跨的比值做自动选择：

\[
\frac{t}{L_c}\leq\frac{1}{20}\Rightarrow \text{K/DKQ},
\qquad
\frac{t}{L_c}>\frac{1}{20}\Rightarrow \text{M/MITC4}.
\]

这里的 `1/20` 是本项目的默认工程判据，不是不可改变的自然定律。实际研究中还要结合误差、材料、几何和目标响应判断。

## 推荐阅读顺序

### 只有 20 分钟

1. 阅读 [`docs/博士面试_数学核心速览.md`](docs/博士面试_数学核心速览.md)。
2. 尝试不看材料说出上面的六步。
3. 阅读 [`docs/博士面试_高频问答.md`](docs/博士面试_高频问答.md) 的前八问。

### 有 1 小时

1. 完成上面的 20 分钟内容。
2. 运行 `\.venv/bin/python examples/step_09_complete_validation.py`。
3. 重点解释输出中的三个现象：
   - `full` 在薄板极限下越来越刚，说明发生剪切锁死；
   - `reduced` 有额外零能模态，说明减缩积分可能引入不稳定；
   - `mitc4` 既保持薄板精度，又保留正确的三个刚体零模态。

### 需要完整算例

- 快速看计算流程：[`output/pdf/plate_core_two_complete_examples_zh.pdf`](output/pdf/plate_core_two_complete_examples_zh.pdf)
- 需要逐公式推导：[`output/pdf/plate_core_two_complete_examples_detailed_derivation_zh.pdf`](output/pdf/plate_core_two_complete_examples_detailed_derivation_zh.pdf)

## 面试时可以这样概括

> 我把板弯曲问题离散为四节点板单元，每个节点使用横向挠度和两个转角。薄板采用 Kirchhoff/DKQ，只保留弯曲能；中厚板采用 Reissner–Mindlin，并用 MITC4 假定剪切场控制剪切锁死。单元刚度由弯曲和剪切应变能积分得到，组装并施加位移边界后求解线性方程，再恢复弯矩、剪力和表面应力。结果通过刚体模态、patch test、平衡、薄板极限、网格收敛和畸变网格进行验证。

## 认知边界

这个仓库目前**不是**通用板壳研究平台。它不包含材料非线性、几何非线性、屈曲、动力、接触、复合材料、膜内自由度或大规模稀疏求解。面试中主动说清边界，比把线性结果说得过大更可信。
